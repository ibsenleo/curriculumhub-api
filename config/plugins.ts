export default () => ({
    "users-permissions": {
    config: {
      register: {
        allowedFields: ["avatar"], // Agrega aquí tus campos personalizados
      },
      // Si también necesitas incluir campos en el login, puedes configurarlos aquí si es necesario.
    },
  },
});
